<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to t('.home'), root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">

      <div class="ms-auto p-2 bd-highlight"> <!-- Positioning the navigation bar item in the far right corner -->
        <div class="btn-toolbar" role="toolbar">
        <% if user_signed_in? %>

          <%= button_to 'Редактировать аккаунт', edit_user_registration_path, method: :get, class: "btn btn-outline-warning", data: { turbo: "false" } %>

          <%= button_to 'Выйти', destroy_user_session_path, method: :delete, class: "btn btn-outline-danger", data: { turbo: "false" } %>

        <% else %>
          <%= link_to 'Зарегистрироваться', new_user_registration_path, class: "btn btn-outline-success", data: { turbo: "false" } %>
          <%= link_to 'Войти', new_user_session_path, class: "btn btn-outline-success", data: { turbo: "false" } %>

        <% end %>
        <div class="text-start">
          <%= render 'layouts/language_switcher' %>
        </div>
        </div>
      </div>
      <div class="text-start">

      </div>
    </div>
  </div>
</nav>
