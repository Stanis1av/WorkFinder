<ul class="navbar-nav">
    <li class="nav-item dropdown">
      <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
        <%= embedded_svg("google-translate", class: "svg-icon") %>
        <span><%= t ('.languages') %></span>
      </a>
      <ul class="dropdown-menu bg-light" aria-labelledby="navbarDropdownMenuLink">
        <li class="">
            <%= link_to({ locale: 'en' }, class: "dropdown-item") do %>
                <%#= embedded_svg("us", class: "svg-flags") %> <!-- connect icon flag -->
                <span>English</span>
            <% end %>
        </li>
        <li>
            <%= link_to({ locale: 'ru' }, class: "dropdown-item") do %>
                <%#= embedded_svg("ru", class: "svg-flags") %>
                <span>Русский</span>
            <% end %>
        </li>
        <li>
            <%= link_to({ locale: 'de' }, class: "dropdown-item") do %>
                <%#= embedded_svg("de", class: "svg-flags") %>
                <span>Deutsche</span>
            <% end %>
        </li>
      </ul>
    </li>
</ul>
