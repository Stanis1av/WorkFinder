<div class="form-devise text-center">

  <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { data: { turbo: false } }) do |f| %>

    <h1 class="h3 mb-3 fw-normal"><%= t('.title') %></h1>

    <div class="">
      <%= link_to user_facebook_omniauth_authorize_path, class: "social-button", id: "facebook-connect", method: :post do %>
        <span><%= t(".log_in_with_provider") + ' Facebook' %></span>
      <% end %>
      <%= link_to user_google_oauth2_omniauth_authorize_path, class: "social-button", id: "google-connect", method: :post do %>
        <span><%= t(".log_in_with_provider") + ' Google' %></span>
      <% end %>
    </div>

    <hr/>

    <div class="form-floating">
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control first", placeholder: t('.enter_email') %>
      <%= f.label :email, t('.enter_email') %>
    </div>

    <div class="form-floating">
      <%= f.password_field :password, autocomplete: "current-password", class: "form-control last", placeholder: t('.enter_password') %>
      <%= f.label :password, t('.enter_password') %>
    </div>

      <% if devise_mapping.rememberable? %>
        <div class="form-group">
          <div class="form-check">
            <label>
              <%= f.check_box :remember_me, class: "form-check-input" %>
              <%= f.label :remember_me, t('.remember_me') %>
          </label>
          </div>
        </div>
      <% end %>
    <div class="actions">
      <%= f.submit t('.sign_in'), class: "w-100 btn btn-lg btn-primary" %>
    </div>
    <br/>

  <% end %>

  <%= render "devise/shared/links" %>

  <p class="mt-5 mb-3 text-muted">&copy; 2021</p>
</div>
